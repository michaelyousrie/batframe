APP_ENV=local
APP_DEBUG=true

# Where data is stored. `json` keeps one readable file per collection under
# storage/database/ and needs nothing installed; `sqlite` needs ext-pdo_sqlite.
# Swapping is this line: no code changes, no migration.
DB_DRIVER=json
